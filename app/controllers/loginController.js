/**
 * Created by Tejas on 8/7/2016.
 */
angular.module("serverConfigApp", []).controller("loginController",
    [
        "$scope", "$http",
        function($scope, $http) {
            $scope.formData = {};
        }
    ]
);